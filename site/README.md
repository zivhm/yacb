# yacb GitHub Pages Site

Minimal Vite + React landing page for GitHub Pages.

## Local development

```bash
cd site
npm install
npm run dev
```

## Build

```bash
cd site
npm run build
```

Output: `site/dist/`

## Deployment

Handled by `.github/workflows/pages.yml` (publishes `site/dist/`).
